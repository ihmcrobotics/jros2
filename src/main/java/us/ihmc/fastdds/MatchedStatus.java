// Targeted by JavaCPP version 1.5.11: DO NOT EDIT THIS FILE

package us.ihmc.fastdds;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static us.ihmc.fastdds.global.fastdds.*;


/** \brief A structure storing a matching status */
@Namespace("eprosima::fastdds::dds") @NoOffset @Properties(inherit = us.ihmc.fastdds.fastddsConfig.class)
public class MatchedStatus extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public MatchedStatus(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public MatchedStatus(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public MatchedStatus position(long position) {
        return (MatchedStatus)super.position(position);
    }
    @Override public MatchedStatus getPointer(long i) {
        return new MatchedStatus((Pointer)this).offsetAddress(i);
    }

    /** \brief Constructor */
    public MatchedStatus() { super((Pointer)null); allocate(); }
    private native void allocate();

    /** \brief Destructor */

    /** \brief Total cumulative count the concerned reader discovered a match with a writer
     *  \details It found a writer for the same topic with a requested QoS that is compatible with that offered by the reader */
    public native int total_count(); public native MatchedStatus total_count(int setter);

    /** \brief The change in total_count since the last time the listener was called or the status was read */
    public native int total_count_change(); public native MatchedStatus total_count_change(int setter);

    /** \brief The number of writers currently matched to the concerned reader */
    public native int current_count(); public native MatchedStatus current_count(int setter);

    /** \brief The change in current_count since the last time the listener was called or the status was read */
    public native int current_count_change(); public native MatchedStatus current_count_change(int setter);
}
