/*
 * This class was automatically generated by jros2.
 * Do not modify this file directly.
 */
package sensor_msgs;

import us.ihmc.fastddsjava.cdr.CDRBuffer;
import us.ihmc.fastddsjava.cdr.idl.*;
import us.ihmc.jros2.ROS2Message;

/**
This service requests that a camera stores the given CameraInfo as that
camera's calibration information.

The width and height in the camera_info field should match what the
camera is currently outputting on its camera_info topic, and the camera
will assume that the region of the imager that is being referred to is
the region that the camera is currently capturing.
<p>Source (sensor_msgs/SetCameraInfo_Request):
<pre>{@code
# This service requests that a camera stores the given CameraInfo as that
# camera's calibration information.
#
# The width and height in the camera_info field should match what the
# camera is currently outputting on its camera_info topic, and the camera
# will assume that the region of the imager that is being referred to is
# the region that the camera is currently capturing.

sensor_msgs/CameraInfo camera_info # The camera_info to store}</pre>
*/
public class SetCameraInfo_Request implements ROS2Message<SetCameraInfo_Request>
{
   public static final java.lang.String name = "sensor_msgs::srv::dds_::SetCameraInfoRequest_";

   private final sensor_msgs.CameraInfo camera_info_; // The camera_info to store

   public SetCameraInfo_Request()
   {
      camera_info_ = new sensor_msgs.CameraInfo();

   }

   @Override
   public int calculateSizeBytes(int currentAlignment)
   {
      int initialAlignment = currentAlignment;

      currentAlignment += camera_info_.calculateSizeBytes(currentAlignment);

      return currentAlignment - initialAlignment;
   }

   @Override
   public void serialize(CDRBuffer buffer)
   {
      camera_info_.serialize(buffer);

   }

   @Override
   public void deserialize(CDRBuffer buffer)
   {
      camera_info_.deserialize(buffer);

   }

   @Override
   public void set(SetCameraInfo_Request from)
   {
      camera_info_.set(from.camera_info_);

   }

   public sensor_msgs.CameraInfo getCameraInfo()
   {
      return camera_info_;
   }


   @Override
   public java.lang.String toString()
   {
      java.lang.StringBuilder builder = new java.lang.StringBuilder();
      builder.append("SetCameraInfo_Request {");
      builder.append("camera_info_=");
      builder.append(camera_info_);

      builder.append("}");
      return builder.toString();
   }
}
