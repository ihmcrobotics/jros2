/*
 * This class was automatically generated by jros2.
 * Do not modify this file directly.
 */
package nav_msgs;

import us.ihmc.fastddsjava.cdr.CDRBuffer;
import us.ihmc.fastddsjava.cdr.idl.*;
import us.ihmc.jros2.ROS2Message;

/**
Get a plan from the current position to the goal Pose
<p>Source (nav_msgs/GetPlan_Request):
<pre>{@code
# Get a plan from the current position to the goal Pose

# The start pose for the plan
geometry_msgs/PoseStamped start

# The final pose of the goal position
geometry_msgs/PoseStamped goal

# If the goal is obstructed, how many meters the planner can
# relax the constraint in x and y before failing.
float32 tolerance}</pre>
*/
public class GetPlan_Request implements ROS2Message<GetPlan_Request>
{
   public static final java.lang.String name = "nav_msgs::srv::dds_::GetPlanRequest_";

   /**
      The start pose for the plan
   */
   private final geometry_msgs.PoseStamped start_;
   /**
      The final pose of the goal position
   */
   private final geometry_msgs.PoseStamped goal_;
   /**
      If the goal is obstructed, how many meters the planner can
      relax the constraint in x and y before failing.
   */
   private float tolerance_;

   public GetPlan_Request()
   {
      start_ = new geometry_msgs.PoseStamped();
      goal_ = new geometry_msgs.PoseStamped();

   }

   @Override
   public int calculateSizeBytes(int currentAlignment)
   {
      int initialAlignment = currentAlignment;

      currentAlignment += start_.calculateSizeBytes(currentAlignment);
      currentAlignment += goal_.calculateSizeBytes(currentAlignment);
      currentAlignment += 4 + CDRBuffer.alignment(currentAlignment, 4); // tolerance_

      return currentAlignment - initialAlignment;
   }

   @Override
   public void serialize(CDRBuffer buffer)
   {
      start_.serialize(buffer);
      goal_.serialize(buffer);
      buffer.writeFloat(tolerance_);

   }

   @Override
   public void deserialize(CDRBuffer buffer)
   {
      start_.deserialize(buffer);
      goal_.deserialize(buffer);
      tolerance_ = buffer.readFloat();

   }

   @Override
   public void set(GetPlan_Request from)
   {
      start_.set(from.start_);
      goal_.set(from.goal_);
      tolerance_ = from.tolerance_;

   }

   public geometry_msgs.PoseStamped getStart()
   {
      return start_;
   }

   public geometry_msgs.PoseStamped getGoal()
   {
      return goal_;
   }

   public float getTolerance()
   {
      return tolerance_;
   }

   public void setTolerance(float tolerance_)
   {
      this.tolerance_ = tolerance_;
   }


   @Override
   public java.lang.String toString()
   {
      java.lang.StringBuilder builder = new java.lang.StringBuilder();
      builder.append("GetPlan_Request {");
      builder.append("start_=");
      builder.append(start_);
      builder.append("goal_=");
      builder.append(goal_);
      builder.append("tolerance_=");
      builder.append(tolerance_);

      builder.append("}");
      return builder.toString();
   }
}
